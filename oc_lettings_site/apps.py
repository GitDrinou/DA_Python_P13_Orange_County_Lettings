from django.apps import AppConfig


class OCLettingsSiteConfig(AppConfig):
    """
    Configure the main Django application for the OC Lettings Site project.

    Attributes:
        name (str): the full Python path tot the application.
    """
    name = 'oc_lettings_site'
