from django.apps import AppConfig


class LettingsConfig(AppConfig):
    """
    Configure the lettings application.

    Attributes:
        name (str): the full Python path of the application.
    """
    name = 'lettings'
